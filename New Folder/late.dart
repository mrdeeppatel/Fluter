void main(List<String> args) {

  //We cannot use it until we give a value inside it.
  //If we try to use the variable we have to git it a value 
  // else it will give run time error
  late  String str1;
  if(1==1){
    str1="values";
  }else{
    
  }
  print(str1);
}